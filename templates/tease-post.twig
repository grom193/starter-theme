{% extends "tease.twig" %}

{% block content %}
	<h2 class="h2"><a href="{{post.link}}">{{post.title}}</a></h2>
		<p>{{post.preview.length(25)}}</p>
	{% if post.thumbnail.src %}
		{{ post.thumbnail|get_responsive_picture('resp800') }}
	{% endif %}
{% endblock %}
